pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh './gradlew release'
            }
        }
        stage('deploy') {
            sh 'sudo java -jar /var/lib/jenkins/workspace/finalRelease/notesApp.jar'
            }
        }
    }
}