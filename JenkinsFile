pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh './gradlew release'
            }
        }

        stage('deploy') {
                    steps {
                        sh 'cd /var/lib/jenkins/workspace/finalRelease'
                        sh 'java -jar noteApp.jar'
                    }
                }
    }
}