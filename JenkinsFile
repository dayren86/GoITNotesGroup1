pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh './gradlew release'
            }
        }

        stage('deploy') {
                    steps {
                        sh 'java -jar /var/lib/jenkins/workspace/finalRelease/noteApp.jar'
                    }
                }
    }
}